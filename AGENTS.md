# Project Instructions

- Use PowerShell on Windows.
- Work from the repository root: `C:\Users\littleviil\Desktop\Diplom`.
- Keep changes small and focused.
- Before editing, inspect existing files and patterns.
- Do not rewrite unrelated code.
- Do not change generated archives like `*.zip`.
- Prefer commands that work from the repository root.
- Run relevant tests before finishing when the project has tests.
- If a command fails, report the exact command and a short error summary.
- Use Russian for user-facing explanations unless the user asks otherwise.

## GSD Notes

- This project is configured for Codex + GSD.
- GSD planning files live in `.planning/`.
- Use text-mode GSD prompts when possible.
- Start project discovery with `$gsd-new-project` once the diploma topic and scope are ready.
- After GSD creates the roadmap, use `$gsd-plan-phase 1`, then `$gsd-execute-phase 1`.
